apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: speedtest
    user: $GF_SECURITY_ADMIN_USER
    password: $GF_SECURITY_ADMIN_PASSWORD
    url: http://influxdb:8086
    isDefault: true
    editable: true
